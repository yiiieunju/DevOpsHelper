# spring.application.name=authentication-server
spring:
  application:
    name: authentication-server 
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8081
    loadbalancer:
      ribbon:
        enabled: false